# Run with: blizzard test examples

assert(2 + 2 == 4, "arithmetic must work");
assert(upper("blizzard") == "BLIZZARD", "string helpers must work");

let value = 21;
assert(value * 2 == 42, "typed variables must preserve their value");
