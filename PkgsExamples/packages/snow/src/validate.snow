# Official Snow library: basic input validation

pub fn non_empty(value):
    return len(trim(value)) > 0

pub fn email(value):
    return contains(value, "@") and contains(value, ".")

pub fn length_between(value, low, high):
    size = len(value)
    return size >= low and size <= high
